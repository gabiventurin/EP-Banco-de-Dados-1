##QUERY 1 - LIKE 

##ENUNCIADO: A consulta realizada abaixo busca saber qual é a Unidade da Rede Hoteleira que teve o maior fauramento em relação a pagamento de reservas no ano de 2023. O campo do resultado "faturamento_reservas" trás uma soma Do valor de entradas de reservas agrupadas por Unidade. Essa consulta é importante para a análise da saúde financeira das Unidades da Rede.


SELECT SUM(ENTRADA.valor_entrada) AS faturamento_reservas, 
       UNIDADE.nome_fantasia
FROM ENTRADA
LEFT JOIN RESERVA ON ENTRADA.id_reserva = RESERVA.id_reserva
LEFT JOIN ACOMODACAO ON RESERVA.id_acomodacao = ACOMODACAO.id_acomodacao
LEFT JOIN UNIDADE ON ACOMODACAO.id_unidade = UNIDADE.id_unidade
WHERE cast(RESERVA.per_dt_fim as varchar) LIKE '________23'
GROUP BY UNIDADE.nome_fantasia;

